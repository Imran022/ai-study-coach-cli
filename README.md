# Study Planner

Small CLI study coach powered by the OpenAI API.

## Run

```bash
pip install -r requirements.txt
```

Create a `.env` file in `study_planner/`:

```env
OPENAI_API_KEY=your_key_here
```

Start the app:

```bash
python main.py
```
